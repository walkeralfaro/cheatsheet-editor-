## 1. Editor Scroll Fix

- [x] 1.1 Add `overflow-hidden` to the left column wrapper in `App.tsx`

## 2. Print Portrait

- [x] 2.1 Change `@page size` from `landscape` to `portrait` in `global.css`

## 3. Dark Theme — CSS & Styles

- [x] 3.1 Add `@variant dark (&.dark)` directive and `.dark` class CSS variable overrides in `global.css`

## 4. Dark Theme — State & Toggle

- [x] 4.1 Add `darkMode` state in `App.tsx` with localStorage persistence and `useEffect` to toggle `.dark` class on `<html>`
- [x] 4.2 Add dark mode toggle button with SVG sun/moon icon in `Toolbar.tsx`
