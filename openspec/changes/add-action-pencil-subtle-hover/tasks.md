## 1. Add pencil to action field and per-cell subtle hover

- [x] 1.1 In `ShortcutCard.tsx`, add pencil button to action cell view mode (same pattern as keys: `opacity-0 group-hover:opacity-100`)
- [x] 1.2 Remove `hover:bg-surface-alt` from `<tr>` and add per-cell subtle hover (`hover:bg-surface-alt/50`) to each `<td>`
- [x] 1.3 Run `npm run build` and confirm no errors
- [ ] 1.4 Verify: both keys and action fields show pencil on hover; hover background is per-cell and subtle
